const initialState = {
  categories: [
    {
      id: 1,
      name: "Petrol",
      disabled: false,
    },
  ],
};

const reducer = (state = initialState, action) => {
  return state;
};

export default reducer;
